<UserControl x:Class="SalonApp.WPF.Views.OverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Pregled termina" 
                   FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="Klijent"/>
                    <ComboBox ItemsSource="{Binding Clients}" 
                              SelectedItem="{Binding FilterClient}"
                              DisplayMemberPath="FullName"
                              IsSynchronizedWithCurrentItem="False"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,10,0">
                    <TextBlock Text="Zaposleni"/>
                    <ComboBox ItemsSource="{Binding StaffMembers}" 
                              SelectedItem="{Binding FilterStaff}"
                              DisplayMemberPath="FullName"
                              IsSynchronizedWithCurrentItem="False"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="0,0,10,0">
                    <TextBlock Text="Datum"/>
                    <DatePicker SelectedDate="{Binding FilterDate}"/>
                </StackPanel>

                <Button Grid.Column="3" Content="Pretrazi" Click="Search_Click" 
                        Padding="15,5" VerticalAlignment="Bottom" Margin="0,0,10,0"/>

                <Button Grid.Column="4" Content="Resetuj" Click="ClearFilters_Click" 
                        Padding="15,5" VerticalAlignment="Bottom"/>
            </Grid>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Appointments}" 
                  AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="40"/>
                <DataGridTextColumn Header="Klijent" Binding="{Binding ClientName}" Width="*"/>
                <DataGridTextColumn Header="Zaposleni" Binding="{Binding StaffName}" Width="*"/>
                <DataGridTextColumn Header="Usluga" Binding="{Binding ServiceName}" Width="*"/>
                <DataGridTextColumn Header="Datum i vreme" Binding="{Binding DateTime, StringFormat=dd.MM.yyyy HH:mm}" Width="130"/>
                <DataGridTextColumn Header="Trajanje" Binding="{Binding DurationInMinutes}" Width="70"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>