<UserControl x:Class="SalonApp.WPF.Views.AppointmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Zakazivanje termina" 
                   FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,10,10">
                    <TextBlock Text="Klijent"/>
                    <ComboBox ItemsSource="{Binding Clients}" 
                              SelectedItem="{Binding SelectedClient}"
                              DisplayMemberPath="FullName"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,10,10">
                    <TextBlock Text="Zaposleni"/>
                    <ComboBox ItemsSource="{Binding StaffMembers}" 
                              SelectedItem="{Binding SelectedStaff}"
                              DisplayMemberPath="FullName"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="2" Margin="0,0,10,10">
                    <TextBlock Text="Usluga"/>
                    <ComboBox ItemsSource="{Binding Services}" 
                              SelectedItem="{Binding SelectedService}"
                              DisplayMemberPath="Name"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="3" Margin="0,0,0,10">
                    <TextBlock Text="Datum"/>
                    <DatePicker SelectedDate="{Binding SelectedDate}"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,10,10">
                    <TextBlock Text="Slobodni termini"/>
                    <ComboBox ItemsSource="{Binding AvailableSlots}" 
                              SelectedItem="{Binding SelectedSlot}"/>
                </StackPanel>

                <Button Grid.Row="1" Grid.Column="1" Content="Zakazi termin" 
                        Click="CreateAppointment_Click" Padding="10,5" 
                        HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="0,0,0,10"/>
            </Grid>
        </StackPanel>
        
        <DataGrid Grid.Row="2" ItemsSource="{Binding Appointments}" 
          AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
          HorizontalScrollBarVisibility="Disabled">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Datum i vreme" Binding="{Binding DateTime, StringFormat=dd.MM.yyyy HH:mm}" Width="150"/>
                <DataGridTextColumn Header="Trajanje (min)" Binding="{Binding DurationInMinutes}" Width="100"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*"/>
                <DataGridTemplateColumn Header="Akcija" Width="110">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Otkazi" Click="CancelAppointment_Click" Tag="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>